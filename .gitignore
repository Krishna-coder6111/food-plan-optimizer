node_modules/
.next/
out/
.env*.local
.env

# Pipeline secrets (never commit your API keys)
data/pipeline/.env
*.tsbuildinfo

# Raw data files (too large for git — download via pipeline)
data/raw/*.csv
data/raw/*.json
data/raw/*.zip
!data/raw/.gitkeep
