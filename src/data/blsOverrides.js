/**
 * BLS price overrides — auto-generated.
 *
 * This file is generated by `python data/pipeline/emit_overrides.py`,
 * which reads `data/processed/bls_prices.json` (produced by the BLS
 * pipeline) and emits per-food per-region price overrides keyed by
 * food id.
 *
 * Workflow to refresh prices:
 *   1. cd data/pipeline && python build_all.py     # downloads + processes
 *   2. python emit_overrides.py                    # writes this file
 *   3. npm run build                               # bake into bundle
 *
 * If you haven't run the pipeline, this is just an empty `{}` and
 * `foods.js` falls back to the hand-curated baseline prices.
 *
 * Shape:
 *   { [foodId]: { us?: number, ne?: number, mw?: number, so?: number, we?: number } }
 */
export const BLS_OVERRIDES = {};
